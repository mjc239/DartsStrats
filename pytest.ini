[pytest]
markers =
    slow: tests that take minutes (brute-force equivalence checks, simulation
        studies). Run everything with `pytest tests/`, or skip these with
        `pytest tests/ -m "not slow"`.
